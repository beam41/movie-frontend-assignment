// @ts-check

/** @typedef {import('prettier')} */
const config = {
  bracketSpacing: true,
  tabWidth: 2,
  useTabs: false,
  singleQuote: true,
  semi: false,
  printWidth: 80,
  trailingComma: 'all',
}

// eslint-disable-next-line unicorn/prefer-module
module.exports = config
